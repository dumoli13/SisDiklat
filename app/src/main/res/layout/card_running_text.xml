<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_cardRunningText"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/margin_small_quarter"
    android:layout_marginRight="@dimen/margin_small_quarter"
    android:layout_marginTop="@dimen/margin_quarter"
    android:paddingBottom="@dimen/padding_small_quarter"
    android:background="@drawable/border_bottom">

    <TextView
        android:id="@+id/tv_runningText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/iv_delete"
        android:layout_alignParentStart="true"
        android:gravity="left"
        android:textSize="12sp"
        android:text="Running Text"
        android:textColor="@color/black"/>

    <WebView
        android:id="@+id/wv_runningText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/iv_delete"
        android:layout_alignParentStart="true"
        tools:ignore="WebViewLayout" />

    <ImageView
        android:id="@+id/iv_delete"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="@dimen/margin_half"
        android:padding="@dimen/margin_small_quarter"
        app:srcCompat="@drawable/cancel" />

</RelativeLayout>
